# 航南生存模拟器：手机紧凑弹窗版

## 更新内容

- 状态栏从左侧移到选项下方，手机上先看人物、事件和 4 个选项。
- 状态栏缩小，放入可折叠区域。
- 每回合行动结果会单独弹窗显示，点击“继续”后才进入下一回合。
- 仍然是单文件版：CSS 和 JS 都在 index.html 里。

## 使用方法

只需要上传并覆盖 GitHub 仓库根目录的 `index.html`。
旧的 CSS/JS 文件可以不管。
上传后按 Ctrl + F5 强制刷新。
